#include<stdio.h>
int main()
{
    int s[10],s1,n,n1,i=0;
    scanf("%d %d",&n,&n1);
    for(i=0;i<n;i++)
    {
        scanf("%d",&s[i]);
    }
    for(i=0;i<n;i++)
    {
        if(s[i]==n1)
        {
            s1=a[i];
            printf("%d",b);
        }
    }
    return 0;
}
